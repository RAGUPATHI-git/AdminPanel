import 'package:flutter/material.dart';

class StudentListMobile extends StatelessWidget {
  const StudentListMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}