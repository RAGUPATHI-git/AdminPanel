import 'package:flutter/material.dart';

class MaintenanceMobile extends StatelessWidget {
  const MaintenanceMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is maintenance screen for mobile"),
      ),
    );
  }
}
