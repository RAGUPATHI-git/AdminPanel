import 'package:flutter/material.dart';

class EditEventsMobile extends StatelessWidget {
  const EditEventsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}