import 'package:flutter/material.dart';

class ProfilesMobile extends StatelessWidget {
  const ProfilesMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}