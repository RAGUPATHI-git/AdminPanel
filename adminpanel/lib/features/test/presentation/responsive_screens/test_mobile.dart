import 'package:flutter/material.dart';

class TestMobile extends StatelessWidget {
  const TestMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}