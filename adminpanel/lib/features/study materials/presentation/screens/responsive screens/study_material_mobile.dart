import 'package:flutter/material.dart';

class StudyMaterialMobile extends StatelessWidget {
  const StudyMaterialMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}