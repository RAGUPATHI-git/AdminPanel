import 'package:flutter/material.dart';

class StudyMaterialDesktop extends StatelessWidget {
  const StudyMaterialDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}