import 'package:flutter/material.dart';

class StudyMaterialTablet extends StatelessWidget {
  const StudyMaterialTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}