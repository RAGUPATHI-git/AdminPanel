import 'package:flutter/material.dart';

class AddNotesTablet extends StatelessWidget {
  const AddNotesTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}