import 'package:flutter/material.dart';

class LoadingTablet extends StatelessWidget {
  const LoadingTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}